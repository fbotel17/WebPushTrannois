runtime: nodejs24

automatic_scaling:
  max_instances: 1

env_variables:
  VAPID_SUBJECT: "mailto:votre.email@example.com"
  PUBLIC_VAPID_KEY: "VOTRE_CLE_VAPID_PUBLIQUE"
  PRIVATE_VAPID_KEY: "VOTRE_CLE_VAPID_PRIVEE"
